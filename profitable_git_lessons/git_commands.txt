# git init
# git add .
# git commit -m 'Your message'
# git status
# git log 'hit q to qiute'
# git checkout (unique commit id)

#Git remote Commands..

# git remote add origin (url)
# git remote -v
# git push origin master
# git pull origin master